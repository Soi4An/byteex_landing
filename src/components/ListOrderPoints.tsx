import { ListItemFull } from "../types/ListItemFull";

type Props = {
  points: ListItemFull;
};

const ListOrderPoints = ({ points }: Props) => (
  <div>
    {'Hello'}
  </div>
);

export default ListOrderPoints;
