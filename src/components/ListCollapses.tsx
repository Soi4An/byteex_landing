type Props = {
  items: {
    title: string,
    text: string,
  }[];
};

function ListCollapses({ items }: Props) {
  return (
    <div>
      {'Hello'}
    </div>
  );
}

export default ListCollapses;
